'use strict';

describe('mojrokovnik.navigation module', function() {

  beforeEach(module('mojrokovnik.navigation'));

  describe('mrMainNavigation controller', function(){

    it('should ....', inject(function($controller) {
      var navigationCtrl = $controller('mrMainNvigationCtrl');
      expect(navigationCtrl).toBeDefined();
    }));

  });
});